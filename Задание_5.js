// 5.5 Массивы и базовые методы работы с ними. Практикуемся с командой debugger Задание 5.
let arr = [3, 8, "apple", false];
console.log(`Количество элементов ${len = arr.length}`); 
for (i=0; i<len; i++) {
	console.log(arr[i]);
 	}